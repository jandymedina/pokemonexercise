import { OnlyLeterDirective } from './only-leter.directive';

describe('OnlyLeterDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyLeterDirective();
    expect(directive).toBeTruthy();
  });
});
