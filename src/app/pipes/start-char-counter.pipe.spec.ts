import { StartCharCounterPipe } from './start-char-counter.pipe';

describe('StartCharCounterPipe', () => {
  it('create an instance', () => {
    const pipe = new StartCharCounterPipe();
    expect(pipe).toBeTruthy();
  });
});
